Quick status check of current project and environment.

1. Run `command git status` and `command git log --oneline -5`
2. Check `docker ps --format "table {{.Names}}\t{{.Status}}"` if Docker is running
3. Check for uncommitted changes, unpushed commits
4. Read last 10 lines of `~/Documents/Obsidian Vault/Claude Code/Activity Log.md`
5. Present a concise summary: branch, recent work, stack health, pending items
